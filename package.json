{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "worktop build src/index.ts --minify",
    "dev": "miniflare --watch",
    "types": "tsc --noEmit"
  },
  "dependencies": {
    "worktop": "0.8.0-next.12"
  },
  "devDependencies": {
    "miniflare": "2.4.0",
    "typescript": "4.6.3",
    "worktop.build": "0.0.5"
  }
}
