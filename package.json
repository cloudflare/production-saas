{
  "private": true,
  "scripts": {
    "build": "cfw build worker -s",
    "deploy": "cfw deploy",
    "types": "tsc --noEmit"
  },
  "devDependencies": {
    "cfw": "0.3.0",
    "typescript": "4.4.3"
  }
}
