{
  "private": true,
  "scripts": {
    "build": "cfw build worker -s --no-minify",
    "deploy": "cfw deploy worker -s",
    "types": "tsc --noEmit"
  },
  "devDependencies": {
    "cfw": "0.3.0",
    "typescript": "4.4.3"
  }
}
