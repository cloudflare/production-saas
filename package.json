{
  "private": true,
  "scripts": {
    "build": "cfw build src -s",
    "deploy": "cfw deploy build -s",
    "dev": "miniflare --watch",
    "types": "tsc --noEmit"
  },
  "dependencies": {
    "worktop": "0.8.0-next.12"
  },
  "devDependencies": {
    "cfw": "0.3.0",
    "typescript": "4.4.3"
    "miniflare": "2.4.0",
  }
}
